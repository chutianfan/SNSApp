<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:layout_gravity="center_horizontal"
    android:gravity="center_horizontal"
    android:orientation="vertical">

    <LinearLayout
        android:id="@id/mvlayout_ll"
        android:layout_width="@dimen/make_video_progress_width"
        android:layout_height="@dimen/make_video_progress_height"
        android:background="@drawable/shape_make_video_bg"
        android:gravity="center"
        android:orientation="vertical">

        <ImageView
            android:id="@id/make_video_progress_iv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/make_video_progress_dialog" />

        <TextView
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/share_dialog_bt_padding"
            android:gravity="center"
            android:text="@string/wait"
            android:textColor="@color/setting_text_color" />
    </LinearLayout>
</LinearLayout>